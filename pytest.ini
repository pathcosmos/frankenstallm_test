[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (mocked external services)
    slow: Slow tests (Bradley-Terry bootstrap etc.)
